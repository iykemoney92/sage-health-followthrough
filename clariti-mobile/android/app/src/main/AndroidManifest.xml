<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- The OAuth return leg. Google refuses OAuth from an embedded WebView,
                 so the provider page opens in a Custom Tab and hands the app back
                 through this scheme — the Android counterpart of iOS's
                 CFBundleURLTypes entry. Must match NATIVE_OAUTH_REDIRECT in
                 clariti-app/lib/auth/oauth.ts. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app.useclariti.mobile" />
            </intent-filter>

            <!-- Android App Links: the counterpart of iOS Universal Links. Email
                 confirmation and password reset must land in the app, not Chrome,
                 or the Supabase session is set in the browser's cookie jar and the
                 app never sees it. android:autoVerify needs
                 https://useclariti.app/.well-known/assetlinks.json to list this
                 package's release signing fingerprint — until it does, Android
                 shows a disambiguation dialog rather than opening directly. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="useclariti.app" android:pathPrefix="/auth/confirm" />
                <data android:scheme="https" android:host="useclariti.app" android:pathPrefix="/auth/callback" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Attaching a document offers "take a photo" alongside picking a file. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 13+ requires this at runtime before notifications (including FCM push) can show. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
