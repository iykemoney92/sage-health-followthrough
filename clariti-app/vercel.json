{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "base=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\"; git cat-file -e \"$base^{commit}\" 2>/dev/null || exit 1; git diff --quiet \"$base\" HEAD -- ./",
  "crons": [
    {
      "path": "/api/agent/trigger-follow-ups",
      "schedule": "0 * * * *"
    }
  ]
}
