# Repositories

Persistence adapters live here.

Start with demo/in-memory repositories if needed, then implement Supabase-backed repositories.

Domain services should depend on repository interfaces, not direct Supabase calls.

