# Integrations

External provider adapters live here.

Expected adapters:

- `elevenlabs` for agent/tool/webhook integration
- `whatsapp` for WhatsApp identifiers and template assumptions
- `anthropic` for structured plan generation
- `supabase` for database clients

Keep provider details out of domain services.

