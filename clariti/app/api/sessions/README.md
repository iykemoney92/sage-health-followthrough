# Sessions API

Routes in this folder will own chat session creation and state.

MVP endpoints:

- `POST /api/sessions`
- `GET /api/sessions`
- `GET /api/sessions/:id`
- `POST /api/sessions/:id/messages`
