"use client";
import Link from "next/link";
import { ArrowLeft, ArrowRight, BellRing, Check, MessageCircle, Upload, HeartPulse, Pill, Moon, BriefcaseBusiness } from "lucide-react";
import { useState } from "react";
import { NuraLogo } from "@/components/nura-logo";
const interests=[['Stress & wellbeing',HeartPulse],['Medication follow-through',Pill],['GP or clinic follow-up',MessageCircle],['Sleep & routine',Moon],['Recovery or return to work',BriefcaseBusiness],['General health organisation',Upload]] as const;
export default function Onboarding(){const[step,setStep]=useState(1);const[selected,setSelected]=useState<string[]>(['Stress & wellbeing']);const toggle=(x:string)=>setSelected(v=>v.includes(x)?v.filter(i=>i!==x):[...v,x]);return <main className="onboarding"><header><NuraLogo compact/><div className="steps"><span className={step>=1?'active':''}/><span className={step>=2?'active':''}/><span className={step>=3?'active':''}/></div><small>Step {step} of 3</small></header><section className="onboarding-card">{step===1&&<><span className="auth-kicker">MAKE NURA YOURS</span><h1>What would you like Nura to help you keep track of?</h1><p>Choose anything that feels relevant. This only shapes your starting experience.</p><div className="interest-grid">{interests.map(([label,Icon])=><button key={label} onClick={()=>toggle(label)} className={selected.includes(label)?'selected':''}><Icon/><span>{label}</span>{selected.includes(label)&&<Check/>}</button>)}</div></>}{step===2&&<><span className="auth-kicker">HOW NURA REACHES YOU</span><h1>Where should Nura follow up with you?</h1><p>You can change this later. Nothing will be sent without your permission.</p><div className="channel-options"><button className="selected"><MessageCircle/><span><b>WhatsApp</b><small>Best for conversational check-ins and voice</small></span><Check/></button><button><BellRing/><span><b>In-app notifications</b><small>Keep everything inside Nura</small></span></button><button><MessageCircle/><span><b>Both</b><small>Use WhatsApp for check-ins and the app for reminders</small></span></button></div></>}{step===3&&<><span className="auth-kicker">START ANYWHERE</span><h1>Tell Nura what&apos;s going on.</h1><p>This becomes your first conversation. Nura will organise anything worth keeping into a Thread.</p><div className="first-intake"><textarea placeholder="For example: I saw my GP today about headaches and they asked me to monitor them for the next few weeks..."/><div><button><Upload/> Add a note</button><span>or just type naturally</span></div></div></>}<footer>{step>1?<button className="secondary-cta" onClick={()=>setStep(step-1)}><ArrowLeft/> Back</button>:<span/>}{step<3?<button className="primary-cta" onClick={()=>setStep(step+1)}>Continue <ArrowRight/></button>:<Link href="/today" className="primary-cta">Meet Nura <ArrowRight/></Link>}</footer></section></main>}